language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install hexo-cli -g
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - npm run deploy

after_script:
  - cd ./public
  - git init
  - git config user.name "zilingyao"
  - git config user.email "1961887727@qq.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - boke

cache:
  directories:
    - node_modules
env:
 global:
   - GH_REF: github.com/zilingyao/zilingyao.github.io.git
